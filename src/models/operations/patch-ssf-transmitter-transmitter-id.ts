/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { ClosedEnum, OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

export const Op = {
  Add: "add",
  Remove: "remove",
  Replace: "replace",
  Move: "move",
  Copy: "copy",
  Test: "test",
} as const;
export type Op = ClosedEnum<typeof Op>;

/**
 * JSON Patch's operation object defined in [RFC 6902: JavaScript Object Notation (JSON) Patch](https://www.rfc-editor.org/rfc/rfc6902.html).
 *
 * @remarks
 */
export type Patch = {
  op: Op;
  path: any;
  value?: any | undefined;
  from?: any | undefined;
};

export type PatchSsfTransmitterTransmitterIdRequestBody = {
  /**
   * JSON Patch defined in [RFC 6902: JavaScript Object Notation (JSON) Patch](https://www.rfc-editor.org/rfc/rfc6902.html).
   *
   * @remarks
   */
  patch: Array<Patch>;
};

export type PatchSsfTransmitterTransmitterIdRequest = {
  /**
   * This header is defined in "[FAPI 1.0 Baseline](https://openid.net/specs/openid-financial-api-part-1-1_0.html)" and "FAPI 2.0 Implementation Advice". If the request includes this header, the response will contain a response header with the same name and the same value. If the request does not include this header, the value of the corresponding response header will be a randomly generated value. This mechanism is designed for tracking communication. When using this header, its value must be a UUID as defined in [RFC 4122](https://www.rfc-editor.org/rfc/rfc4122.html).
   *
   * @remarks
   */
  xFapiInteractionId?: any | undefined;
  /**
   * This query parameter enables pretty-printing when the response content type is JSON. If `pretty=true` (case-insensitive), the response is formatted for readability. If `pretty` is omitted or set to any other value, pretty-printing is disabled.
   *
   * @remarks
   */
  pretty?: any | undefined;
  /**
   * The transmitter ID.
   *
   * @remarks
   */
  transmitterId: any;
  body?: PatchSsfTransmitterTransmitterIdRequestBody | undefined;
};

export const PatchSsfTransmitterTransmitterIdNotFoundStatus = {
  Success: "success",
  Failure: "failure",
  BadRequest: "bad_request",
  Unauthorized: "unauthorized",
  Forbidden: "forbidden",
  NotFound: "not_found",
  Conflict: "conflict",
  ServerError: "server_error",
  ServiceUnavailable: "service_unavailable",
} as const;
export type PatchSsfTransmitterTransmitterIdNotFoundStatus = OpenEnum<
  typeof PatchSsfTransmitterTransmitterIdNotFoundStatus
>;

export type PatchSsfTransmitterTransmitterIdNotFoundResult = {
  status?: PatchSsfTransmitterTransmitterIdNotFoundStatus | undefined;
  code?: any | undefined;
  message?: any | undefined;
};

export const PatchSsfTransmitterTransmitterIdBadRequestStatus = {
  Success: "success",
  Failure: "failure",
  BadRequest: "bad_request",
  Unauthorized: "unauthorized",
  Forbidden: "forbidden",
  NotFound: "not_found",
  Conflict: "conflict",
  ServerError: "server_error",
  ServiceUnavailable: "service_unavailable",
} as const;
export type PatchSsfTransmitterTransmitterIdBadRequestStatus = OpenEnum<
  typeof PatchSsfTransmitterTransmitterIdBadRequestStatus
>;

export type PatchSsfTransmitterTransmitterIdBadRequestResult = {
  status?: PatchSsfTransmitterTransmitterIdBadRequestStatus | undefined;
  code?: any | undefined;
  message?: any | undefined;
};

export const PatchSsfTransmitterTransmitterIdStatus = {
  Success: "success",
  Failure: "failure",
  BadRequest: "bad_request",
  Unauthorized: "unauthorized",
  Forbidden: "forbidden",
  NotFound: "not_found",
  Conflict: "conflict",
  ServerError: "server_error",
  ServiceUnavailable: "service_unavailable",
} as const;
export type PatchSsfTransmitterTransmitterIdStatus = OpenEnum<
  typeof PatchSsfTransmitterTransmitterIdStatus
>;

export type PatchSsfTransmitterTransmitterIdResult = {
  status?: PatchSsfTransmitterTransmitterIdStatus | undefined;
  code?: any | undefined;
  message?: any | undefined;
};

/**
 * The creator of the transmitter.
 */
export type PatchSsfTransmitterTransmitterIdCreator = {
  /**
   * The issuer of the access token.
   */
  iss?: any | undefined;
  /**
   * The subject of the access token.
   */
  sub?: any | undefined;
  /**
   * The client ID of the access token.
   */
  clientId?: any | undefined;
  /**
   * The identifier of the access token.
   */
  jti?: any | undefined;
  /**
   * The base64url-encoded SHA-256 hash of the access token.
   */
  hash?: any | undefined;
};

export type PatchSsfTransmitterTransmitterIdAuthorizationScheme = {
  specUrn: any;
};

/**
 * OPTIONAL. A string indicating the default behavior of newly created streams. If present, the value MUST be either `"ALL"` or `"NONE"`. If not provided, the Transmitter behavior in this regard is unspecified.
 *
 * @remarks
 * - `"ALL"` indicates that any subjects that are appropriate for the
 *   stream are added to the stream by default. The Receiver MAY remove
 *   subjects from the stream via the `remove_subject_endpoint`, causing
 *   events for those subjects to _not_ be transmitted. The Receiver MAY
 *   re-add any subjects removed this way via the `add_subject_endpoint`.
 *
 * - `"NONE"` indicates that no subjects are added by default. The Receiver
 *   MAY add subjects to the stream via the `add_subject_endpoint`,
 *   causing only events for those subjects to be transmitted. The Receiver
 *   MAY remove subjects added this way via the `remove_subject_endpoint`.
 */
export const PatchSsfTransmitterTransmitterIdDefaultSubjects = {
  All: "ALL",
  None: "NONE",
} as const;
/**
 * OPTIONAL. A string indicating the default behavior of newly created streams. If present, the value MUST be either `"ALL"` or `"NONE"`. If not provided, the Transmitter behavior in this regard is unspecified.
 *
 * @remarks
 * - `"ALL"` indicates that any subjects that are appropriate for the
 *   stream are added to the stream by default. The Receiver MAY remove
 *   subjects from the stream via the `remove_subject_endpoint`, causing
 *   events for those subjects to _not_ be transmitted. The Receiver MAY
 *   re-add any subjects removed this way via the `add_subject_endpoint`.
 *
 * - `"NONE"` indicates that no subjects are added by default. The Receiver
 *   MAY add subjects to the stream via the `add_subject_endpoint`,
 *   causing only events for those subjects to be transmitted. The Receiver
 *   MAY remove subjects added this way via the `remove_subject_endpoint`.
 */
export type PatchSsfTransmitterTransmitterIdDefaultSubjects = OpenEnum<
  typeof PatchSsfTransmitterTransmitterIdDefaultSubjects
>;

/**
 * Transmitter Configuration Metadata defined in [OpenID Shared Signals Framework Specification 1.0](https://openid.net/specs/openid-sharedsignals-framework-1_0.html).
 *
 * @remarks
 */
export type PatchSsfTransmitterTransmitterIdMetadata = {
  /**
   * OPTIONAL. A version identifying the implementer's draft or final specification implemented by the Transmitter. This includes the numerical portion of the spec version as described in the document [[NAMINGCONVENTION](https://openid.net/wg/resources/naming-and-contents-of-specifications/)].
   *
   * @remarks
   */
  specVersion?: any | undefined;
  /**
   * REQUIRED. URL using the `https` scheme with no query or fragment component that the Transmitter asserts as its Issuer Identifier. This MUST be identical to the `iss` claim value in Security Event Tokens issued from this Transmitter.
   *
   * @remarks
   */
  issuer: any;
  /**
   * OPTIONAL. URL of the Transmitter's JSON Web Key Set [[RFC7517](https://www.rfc-editor.org/rfc/rfc7517.html)] document. This contains the signing key(s) the Receiver uses to validate signatures from the Transmitter. This value MUST be specified if the Transmitter intends to generate signed JWTs. If present, this URL MUST use HTTP over TLS [[RFC9110](https://www.rfc-editor.org/rfc/rfc9110.html)].
   *
   * @remarks
   */
  jwksUri?: any | undefined;
  /**
   * RECOMMENDED. List of supported delivery method URIs.
   *
   * @remarks
   */
  deliveryMethodsSupported?: Array<any> | undefined;
  /**
   * OPTIONAL. The URL of the Configuration Endpoint. If present, this URL MUST use HTTP over TLS [[RFC9110](https://www.rfc-editor.org/rfc/rfc9110.html)].
   *
   * @remarks
   */
  configurationEndpoint?: any | undefined;
  /**
   * OPTIONAL. The URL of the Status Endpoint. If present, this URL MUST use HTTP over TLS [[RFC9110](https://www.rfc-editor.org/rfc/rfc9110.html)].
   *
   * @remarks
   */
  statusEndpoint?: any | undefined;
  /**
   * OPTIONAL. The URL of the Add Subject Endpoint. If present, this URL MUST use HTTP over TLS [[RFC9110](https://www.rfc-editor.org/rfc/rfc9110.html)].
   *
   * @remarks
   */
  addSubjectEndpoint?: any | undefined;
  /**
   * OPTIONAL. The URL of the Remove Subject Endpoint. If present, this URL MUST use HTTP over TLS [[RFC9110](https://www.rfc-editor.org/rfc/rfc9110.html)].
   *
   * @remarks
   */
  removeSubjectEndpoint?: any | undefined;
  /**
   * OPTIONAL. The URL of the Verification Endpoint. If present, this URL MUST use HTTP over TLS [[RFC9110](https://www.rfc-editor.org/rfc/rfc9110.html)].
   *
   * @remarks
   */
  verificationEndpoint?: any | undefined;
  /**
   * OPTIONAL. An array of member names in a Complex Subject which, if present in a Subject Member in an event, MUST be interpreted by a Receiver.
   *
   * @remarks
   */
  criticalSubjectMembers?: Array<any> | undefined;
  /**
   * OPTIONAL. An array of JSON objects that specify the supported authorization scheme properties. To enable seamless discovery of configurations, the service provider SHOULD, with the appropriate security considerations, make the `authorization_schemes` attribute publicly accessible without prior authentication.
   *
   * @remarks
   */
  authorizationSchemes?:
    | Array<PatchSsfTransmitterTransmitterIdAuthorizationScheme>
    | undefined;
  /**
   * OPTIONAL. A string indicating the default behavior of newly created streams. If present, the value MUST be either `"ALL"` or `"NONE"`. If not provided, the Transmitter behavior in this regard is unspecified.
   *
   * @remarks
   * - `"ALL"` indicates that any subjects that are appropriate for the
   *   stream are added to the stream by default. The Receiver MAY remove
   *   subjects from the stream via the `remove_subject_endpoint`, causing
   *   events for those subjects to _not_ be transmitted. The Receiver MAY
   *   re-add any subjects removed this way via the `add_subject_endpoint`.
   *
   * - `"NONE"` indicates that no subjects are added by default. The Receiver
   *   MAY add subjects to the stream via the `add_subject_endpoint`,
   *   causing only events for those subjects to be transmitted. The Receiver
   *   MAY remove subjects added this way via the `remove_subject_endpoint`.
   */
  defaultSubjects: PatchSsfTransmitterTransmitterIdDefaultSubjects;
};

/**
 * The HTTP authentication scheme.
 */
export const PatchSsfTransmitterTransmitterIdScheme = {
  Basic: "Basic",
  Bearer: "Bearer",
  DPoP: "DPoP",
} as const;
/**
 * The HTTP authentication scheme.
 */
export type PatchSsfTransmitterTransmitterIdScheme = OpenEnum<
  typeof PatchSsfTransmitterTransmitterIdScheme
>;

/**
 * Configuration for access token introspection ([RFC 7662](https://www.rfc-editor.org/rfc/rfc7662.html)).
 *
 * @remarks
 */
export type PatchSsfTransmitterTransmitterIdIntrospectionConfiguration = {
  /**
   * The HTTP authentication scheme.
   */
  scheme?: PatchSsfTransmitterTransmitterIdScheme | undefined;
  /**
   * The user ID, required for the Basic Authentication.
   */
  userId?: any | undefined;
  /**
   * The password, required for the Basic Authentication.
   */
  password?: any | undefined;
  /**
   * The access token, required for the Bearer or DPoP Authentication.
   */
  accessToken?: any | undefined;
  /**
   * The identifier of the authorization server.
   */
  issuer: any;
};

export type PatchSsfTransmitterTransmitterIdSettings = {
  /**
   * The name of this Transmitter
   */
  name?: any | undefined;
  /**
   * The description about this Transmitter
   */
  description?: any | undefined;
  /**
   * This boolean flag indicates whether FAPI 2.0 compliance is required.
   *
   * @remarks
   *
   * If this flag is set to `true`, the requirements defined in the
   * [FAPI 2.0 Security Profile](https://openid.net/specs/fapi-security-profile-2_0-final.html)
   * are enforced. One notable requirement is that access tokens must
   * be sender-constrained using either MTLS
   * ([RFC 8705](https://www.rfc-editor.org/rfc/rfc8705.html)) or DPoP
   * ([RFC 9449](https://www.rfc-editor.org/rfc/rfc9449.html)).
   */
  fapi2Compliant: any;
  /**
   * This boolean flag indicates whether access tokens must be
   *
   * @remarks
   * sender-constrained using the DPoP
   * ([RFC 9449](https://www.rfc-editor.org/rfc/rfc9449.html)) mechanism.
   */
  dpopRequired: any;
  /**
   * This boolean flag indicates whether DPoP proof JWTs must include the
   *
   * @remarks
   * `nonce` claim.
   *
   * If this flag is set to `true`, the DPoP proof JWT presented with a
   * DPoP-key-bound access token must include the `nonce` claim. Otherwise,
   * the server will return a `401 Unauthorized` response with the error
   * code `use_dpop_nonce` and a `DPoP-Nonce` header. If the client receives
   * this error, it must regenerate a DPoP proof JWT including the `nonce`
   * claim, using the nonce value specified in the `DPop-Nonce` header, and
   * retry the request.
   *
   * Also note that the nonce value expires periodically. This means that
   * the client will periodically receive a `401 Unauthorized` response
   * with the error code `invalid_dpop_proof` and a `DPoP-Nonce` header.
   * If the client receives this error, it must regenerate a DPoP proof JWT
   * including the `nonce` claim with the updated nonce value specified in
   * the `DPoP-Nonce` header and retry the request.
   *
   * See [RFC 9449](https://www.rfc-editor.org/rfc/rfc9449.html) for details
   * about the DPoP nonce.
   */
  dpopNonceRequired: any;
  /**
   * The maximum clock skew in seconds. This value is taken into account
   *
   * @remarks
   * when validating time-related properties, such as the expiration time
   * of an access token.
   *
   * Note that when FAPI 2.0 compliance is required (i.e., when the
   * `fapi2_compliant` flag is set to `true`), the maximum clock skew is
   * adjusted to ensure that it falls within the range of 10 to 60 seconds.
   */
  maxClockSkewSeconds?: any | undefined;
  /**
   * The list of authorization servers this Transmitter queries for access token information.
   *
   * @remarks
   */
  authorizationServers?: Array<any> | undefined;
  /**
   * The list of configurations for access token introspection.
   *
   * @remarks
   */
  introspectionConfigurations?:
    | Array<PatchSsfTransmitterTransmitterIdIntrospectionConfiguration>
    | undefined;
  /**
   * OPTIONAL. An array of URIs identifying the set of events supported by the Transmitter for a Receiver. If omitted, Event Transmitters SHOULD make this set available to the Event Receiver via some other means (e.g. publishing it in online documentation).
   *
   * @remarks
   */
  eventsSupported?: Array<any> | undefined;
  /**
   * The default value of `min_verification_interval` in a Stream configuration.
   *
   * @remarks
   */
  minVerificationIntervalDefault?: any | undefined;
  /**
   * The default value of `inactivity_timeout` in a Stream configuration.
   *
   * @remarks
   */
  inactivityTimeoutDefault?: any | undefined;
  /**
   * OPTIONAL. The URL of the Poll Endpoint.
   *
   * @remarks
   *
   * If the receiver specifies the delivery method as POLL (`urn:ietf:rfc:8936`)
   * in the stream creation request, the value of this property is used as the
   * `endpoint_url`. If the property value contains `STREAM_ID`, it will be
   * replaced with the newly issued stream ID.
   */
  pollEndpoint?: any | undefined;
};

export type PatchSsfTransmitterTransmitterIdTransmitter = {
  /**
   * The time at which the transmitter was created, in the format of `YYYY-MM-DDThh:mm:ss`.
   *
   * @remarks
   */
  createdAt?: any | undefined;
  /**
   * The time at which the transmitter was updated, in the format of `YYYY-MM-DDThh:mm:ss`.
   *
   * @remarks
   */
  updatedAt?: any | undefined;
  /**
   * The creator of the transmitter.
   */
  creator?: PatchSsfTransmitterTransmitterIdCreator | undefined;
  /**
   * The Transmitter ID assigned by the Arena Core Server. The value has 240-bit entropy and is encoded in the base32hex format, resulting in a 48-character lowercase string.
   *
   * @remarks
   * Strictly speaking, since the first 10 bits of the data are overwritten to ensure that the resulting string always starts with `tr`, the actual entropy is 230 bits. The `tr` here denotes Transmitter.
   * The purpose of this process is to enable someone encountering a 48-character base32hex-encoded string beginning with `tr` to infer that it might be a Transmitter ID. Conversely, a random string not starting with `tr` can be identified as not being a Transmitter ID.
   */
  transmitterId?: any | undefined;
  /**
   * Transmitter Configuration Metadata defined in [OpenID Shared Signals Framework Specification 1.0](https://openid.net/specs/openid-sharedsignals-framework-1_0.html).
   *
   * @remarks
   */
  metadata: PatchSsfTransmitterTransmitterIdMetadata;
  settings?: PatchSsfTransmitterTransmitterIdSettings | undefined;
};

/**
 * Successfully applied the patch to the transmitter.
 */
export type PatchSsfTransmitterTransmitterIdResponseBody = {
  result?: PatchSsfTransmitterTransmitterIdResult | undefined;
  transmitter?: PatchSsfTransmitterTransmitterIdTransmitter | undefined;
};

export type PatchSsfTransmitterTransmitterIdResponse = {
  headers: { [k: string]: Array<string> };
  result: PatchSsfTransmitterTransmitterIdResponseBody;
};

/** @internal */
export const Op$outboundSchema: z.ZodMiniEnum<typeof Op> = z.enum(Op);

/** @internal */
export type Patch$Outbound = {
  op: string;
  path: any;
  value?: any | undefined;
  from?: any | undefined;
};

/** @internal */
export const Patch$outboundSchema: z.ZodMiniType<Patch$Outbound, Patch> = z
  .object({
    op: Op$outboundSchema,
    path: z.any(),
    value: z.optional(z.any()),
    from: z.optional(z.any()),
  });

export function patchToJSON(patch: Patch): string {
  return JSON.stringify(Patch$outboundSchema.parse(patch));
}

/** @internal */
export type PatchSsfTransmitterTransmitterIdRequestBody$Outbound = {
  patch: Array<Patch$Outbound>;
};

/** @internal */
export const PatchSsfTransmitterTransmitterIdRequestBody$outboundSchema:
  z.ZodMiniType<
    PatchSsfTransmitterTransmitterIdRequestBody$Outbound,
    PatchSsfTransmitterTransmitterIdRequestBody
  > = z.object({
    patch: z.array(z.lazy(() => Patch$outboundSchema)),
  });

export function patchSsfTransmitterTransmitterIdRequestBodyToJSON(
  patchSsfTransmitterTransmitterIdRequestBody:
    PatchSsfTransmitterTransmitterIdRequestBody,
): string {
  return JSON.stringify(
    PatchSsfTransmitterTransmitterIdRequestBody$outboundSchema.parse(
      patchSsfTransmitterTransmitterIdRequestBody,
    ),
  );
}

/** @internal */
export type PatchSsfTransmitterTransmitterIdRequest$Outbound = {
  "x-fapi-interaction-id"?: any | undefined;
  pretty: any;
  transmitter_id: any;
  body?: PatchSsfTransmitterTransmitterIdRequestBody$Outbound | undefined;
};

/** @internal */
export const PatchSsfTransmitterTransmitterIdRequest$outboundSchema:
  z.ZodMiniType<
    PatchSsfTransmitterTransmitterIdRequest$Outbound,
    PatchSsfTransmitterTransmitterIdRequest
  > = z.pipe(
    z.object({
      xFapiInteractionId: z.optional(z.any()),
      pretty: z.any(),
      transmitterId: z.any(),
      body: z.optional(
        z.lazy(() =>
          PatchSsfTransmitterTransmitterIdRequestBody$outboundSchema
        ),
      ),
    }),
    z.transform((v) => {
      return remap$(v, {
        xFapiInteractionId: "x-fapi-interaction-id",
        transmitterId: "transmitter_id",
      });
    }),
  );

export function patchSsfTransmitterTransmitterIdRequestToJSON(
  patchSsfTransmitterTransmitterIdRequest:
    PatchSsfTransmitterTransmitterIdRequest,
): string {
  return JSON.stringify(
    PatchSsfTransmitterTransmitterIdRequest$outboundSchema.parse(
      patchSsfTransmitterTransmitterIdRequest,
    ),
  );
}

/** @internal */
export const PatchSsfTransmitterTransmitterIdNotFoundStatus$inboundSchema:
  z.ZodMiniType<PatchSsfTransmitterTransmitterIdNotFoundStatus, unknown> =
    openEnums.inboundSchema(PatchSsfTransmitterTransmitterIdNotFoundStatus);

/** @internal */
export const PatchSsfTransmitterTransmitterIdNotFoundResult$inboundSchema:
  z.ZodMiniType<PatchSsfTransmitterTransmitterIdNotFoundResult, unknown> = z
    .object({
      status: types.optional(
        PatchSsfTransmitterTransmitterIdNotFoundStatus$inboundSchema,
      ),
      code: types.optional(z.any()),
      message: types.optional(z.any()),
    });

export function patchSsfTransmitterTransmitterIdNotFoundResultFromJSON(
  jsonString: string,
): SafeParseResult<
  PatchSsfTransmitterTransmitterIdNotFoundResult,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      PatchSsfTransmitterTransmitterIdNotFoundResult$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'PatchSsfTransmitterTransmitterIdNotFoundResult' from JSON`,
  );
}

/** @internal */
export const PatchSsfTransmitterTransmitterIdBadRequestStatus$inboundSchema:
  z.ZodMiniType<PatchSsfTransmitterTransmitterIdBadRequestStatus, unknown> =
    openEnums.inboundSchema(PatchSsfTransmitterTransmitterIdBadRequestStatus);

/** @internal */
export const PatchSsfTransmitterTransmitterIdBadRequestResult$inboundSchema:
  z.ZodMiniType<PatchSsfTransmitterTransmitterIdBadRequestResult, unknown> = z
    .object({
      status: types.optional(
        PatchSsfTransmitterTransmitterIdBadRequestStatus$inboundSchema,
      ),
      code: types.optional(z.any()),
      message: types.optional(z.any()),
    });

export function patchSsfTransmitterTransmitterIdBadRequestResultFromJSON(
  jsonString: string,
): SafeParseResult<
  PatchSsfTransmitterTransmitterIdBadRequestResult,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      PatchSsfTransmitterTransmitterIdBadRequestResult$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'PatchSsfTransmitterTransmitterIdBadRequestResult' from JSON`,
  );
}

/** @internal */
export const PatchSsfTransmitterTransmitterIdStatus$inboundSchema:
  z.ZodMiniType<PatchSsfTransmitterTransmitterIdStatus, unknown> = openEnums
    .inboundSchema(PatchSsfTransmitterTransmitterIdStatus);

/** @internal */
export const PatchSsfTransmitterTransmitterIdResult$inboundSchema:
  z.ZodMiniType<PatchSsfTransmitterTransmitterIdResult, unknown> = z.object({
    status: types.optional(
      PatchSsfTransmitterTransmitterIdStatus$inboundSchema,
    ),
    code: types.optional(z.any()),
    message: types.optional(z.any()),
  });

export function patchSsfTransmitterTransmitterIdResultFromJSON(
  jsonString: string,
): SafeParseResult<PatchSsfTransmitterTransmitterIdResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      PatchSsfTransmitterTransmitterIdResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PatchSsfTransmitterTransmitterIdResult' from JSON`,
  );
}

/** @internal */
export const PatchSsfTransmitterTransmitterIdCreator$inboundSchema:
  z.ZodMiniType<PatchSsfTransmitterTransmitterIdCreator, unknown> = z.pipe(
    z.object({
      iss: types.optional(z.any()),
      sub: types.optional(z.any()),
      client_id: types.optional(z.any()),
      jti: types.optional(z.any()),
      hash: types.optional(z.any()),
    }),
    z.transform((v) => {
      return remap$(v, {
        "client_id": "clientId",
      });
    }),
  );

export function patchSsfTransmitterTransmitterIdCreatorFromJSON(
  jsonString: string,
): SafeParseResult<
  PatchSsfTransmitterTransmitterIdCreator,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      PatchSsfTransmitterTransmitterIdCreator$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'PatchSsfTransmitterTransmitterIdCreator' from JSON`,
  );
}

/** @internal */
export const PatchSsfTransmitterTransmitterIdAuthorizationScheme$inboundSchema:
  z.ZodMiniType<PatchSsfTransmitterTransmitterIdAuthorizationScheme, unknown> =
    z.pipe(
      z.object({
        spec_urn: z.any(),
      }),
      z.transform((v) => {
        return remap$(v, {
          "spec_urn": "specUrn",
        });
      }),
    );

export function patchSsfTransmitterTransmitterIdAuthorizationSchemeFromJSON(
  jsonString: string,
): SafeParseResult<
  PatchSsfTransmitterTransmitterIdAuthorizationScheme,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      PatchSsfTransmitterTransmitterIdAuthorizationScheme$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'PatchSsfTransmitterTransmitterIdAuthorizationScheme' from JSON`,
  );
}

/** @internal */
export const PatchSsfTransmitterTransmitterIdDefaultSubjects$inboundSchema:
  z.ZodMiniType<PatchSsfTransmitterTransmitterIdDefaultSubjects, unknown> =
    openEnums.inboundSchema(PatchSsfTransmitterTransmitterIdDefaultSubjects);

/** @internal */
export const PatchSsfTransmitterTransmitterIdMetadata$inboundSchema:
  z.ZodMiniType<PatchSsfTransmitterTransmitterIdMetadata, unknown> = z.pipe(
    z.object({
      spec_version: types.optional(z.any()),
      issuer: z.any(),
      jwks_uri: types.optional(z.any()),
      delivery_methods_supported: types.optional(z.array(z.any())),
      configuration_endpoint: types.optional(z.any()),
      status_endpoint: types.optional(z.any()),
      add_subject_endpoint: types.optional(z.any()),
      remove_subject_endpoint: types.optional(z.any()),
      verification_endpoint: types.optional(z.any()),
      critical_subject_members: types.optional(z.array(z.any())),
      authorization_schemes: types.optional(z.array(z.lazy(() =>
        PatchSsfTransmitterTransmitterIdAuthorizationScheme$inboundSchema
      ))),
      default_subjects: z._default(
        PatchSsfTransmitterTransmitterIdDefaultSubjects$inboundSchema,
        "NONE",
      ),
    }),
    z.transform((v) => {
      return remap$(v, {
        "spec_version": "specVersion",
        "jwks_uri": "jwksUri",
        "delivery_methods_supported": "deliveryMethodsSupported",
        "configuration_endpoint": "configurationEndpoint",
        "status_endpoint": "statusEndpoint",
        "add_subject_endpoint": "addSubjectEndpoint",
        "remove_subject_endpoint": "removeSubjectEndpoint",
        "verification_endpoint": "verificationEndpoint",
        "critical_subject_members": "criticalSubjectMembers",
        "authorization_schemes": "authorizationSchemes",
        "default_subjects": "defaultSubjects",
      });
    }),
  );

export function patchSsfTransmitterTransmitterIdMetadataFromJSON(
  jsonString: string,
): SafeParseResult<
  PatchSsfTransmitterTransmitterIdMetadata,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      PatchSsfTransmitterTransmitterIdMetadata$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'PatchSsfTransmitterTransmitterIdMetadata' from JSON`,
  );
}

/** @internal */
export const PatchSsfTransmitterTransmitterIdScheme$inboundSchema:
  z.ZodMiniType<PatchSsfTransmitterTransmitterIdScheme, unknown> = openEnums
    .inboundSchema(PatchSsfTransmitterTransmitterIdScheme);

/** @internal */
export const PatchSsfTransmitterTransmitterIdIntrospectionConfiguration$inboundSchema:
  z.ZodMiniType<
    PatchSsfTransmitterTransmitterIdIntrospectionConfiguration,
    unknown
  > = z.pipe(
    z.object({
      scheme: types.optional(
        PatchSsfTransmitterTransmitterIdScheme$inboundSchema,
      ),
      user_id: types.optional(z.any()),
      password: types.optional(z.any()),
      access_token: types.optional(z.any()),
      issuer: z.any(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "user_id": "userId",
        "access_token": "accessToken",
      });
    }),
  );

export function patchSsfTransmitterTransmitterIdIntrospectionConfigurationFromJSON(
  jsonString: string,
): SafeParseResult<
  PatchSsfTransmitterTransmitterIdIntrospectionConfiguration,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      PatchSsfTransmitterTransmitterIdIntrospectionConfiguration$inboundSchema
        .parse(JSON.parse(x)),
    `Failed to parse 'PatchSsfTransmitterTransmitterIdIntrospectionConfiguration' from JSON`,
  );
}

/** @internal */
export const PatchSsfTransmitterTransmitterIdSettings$inboundSchema:
  z.ZodMiniType<PatchSsfTransmitterTransmitterIdSettings, unknown> = z.pipe(
    z.object({
      name: types.optional(z.any()),
      description: types.optional(z.any()),
      fapi2_compliant: z.any(),
      dpop_required: z.any(),
      dpop_nonce_required: z.any(),
      max_clock_skew_seconds: types.optional(z.any()),
      authorization_servers: types.optional(z.array(z.any())),
      introspection_configurations: types.optional(z.array(z.lazy(() =>
        PatchSsfTransmitterTransmitterIdIntrospectionConfiguration$inboundSchema
      ))),
      events_supported: types.optional(z.array(z.any())),
      min_verification_interval_default: types.optional(z.any()),
      inactivity_timeout_default: types.optional(z.any()),
      poll_endpoint: types.optional(z.any()),
    }),
    z.transform((v) => {
      return remap$(v, {
        "fapi2_compliant": "fapi2Compliant",
        "dpop_required": "dpopRequired",
        "dpop_nonce_required": "dpopNonceRequired",
        "max_clock_skew_seconds": "maxClockSkewSeconds",
        "authorization_servers": "authorizationServers",
        "introspection_configurations": "introspectionConfigurations",
        "events_supported": "eventsSupported",
        "min_verification_interval_default": "minVerificationIntervalDefault",
        "inactivity_timeout_default": "inactivityTimeoutDefault",
        "poll_endpoint": "pollEndpoint",
      });
    }),
  );

export function patchSsfTransmitterTransmitterIdSettingsFromJSON(
  jsonString: string,
): SafeParseResult<
  PatchSsfTransmitterTransmitterIdSettings,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      PatchSsfTransmitterTransmitterIdSettings$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'PatchSsfTransmitterTransmitterIdSettings' from JSON`,
  );
}

/** @internal */
export const PatchSsfTransmitterTransmitterIdTransmitter$inboundSchema:
  z.ZodMiniType<PatchSsfTransmitterTransmitterIdTransmitter, unknown> = z.pipe(
    z.object({
      created_at: types.optional(z.any()),
      updated_at: types.optional(z.any()),
      creator: types.optional(z.lazy(() =>
        PatchSsfTransmitterTransmitterIdCreator$inboundSchema
      )),
      transmitter_id: types.optional(z.any()),
      metadata: z.lazy(() =>
        PatchSsfTransmitterTransmitterIdMetadata$inboundSchema
      ),
      settings: types.optional(z.lazy(() =>
        PatchSsfTransmitterTransmitterIdSettings$inboundSchema
      )),
    }),
    z.transform((v) => {
      return remap$(v, {
        "created_at": "createdAt",
        "updated_at": "updatedAt",
        "transmitter_id": "transmitterId",
      });
    }),
  );

export function patchSsfTransmitterTransmitterIdTransmitterFromJSON(
  jsonString: string,
): SafeParseResult<
  PatchSsfTransmitterTransmitterIdTransmitter,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      PatchSsfTransmitterTransmitterIdTransmitter$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'PatchSsfTransmitterTransmitterIdTransmitter' from JSON`,
  );
}

/** @internal */
export const PatchSsfTransmitterTransmitterIdResponseBody$inboundSchema:
  z.ZodMiniType<PatchSsfTransmitterTransmitterIdResponseBody, unknown> = z
    .object({
      result: types.optional(
        z.lazy(() => PatchSsfTransmitterTransmitterIdResult$inboundSchema),
      ),
      transmitter: types.optional(
        z.lazy(() => PatchSsfTransmitterTransmitterIdTransmitter$inboundSchema),
      ),
    });

export function patchSsfTransmitterTransmitterIdResponseBodyFromJSON(
  jsonString: string,
): SafeParseResult<
  PatchSsfTransmitterTransmitterIdResponseBody,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      PatchSsfTransmitterTransmitterIdResponseBody$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'PatchSsfTransmitterTransmitterIdResponseBody' from JSON`,
  );
}

/** @internal */
export const PatchSsfTransmitterTransmitterIdResponse$inboundSchema:
  z.ZodMiniType<PatchSsfTransmitterTransmitterIdResponse, unknown> = z.pipe(
    z.object({
      Headers: z._default(z.record(z.string(), z.array(z.string())), {}),
      Result: z.lazy(() =>
        PatchSsfTransmitterTransmitterIdResponseBody$inboundSchema
      ),
    }),
    z.transform((v) => {
      return remap$(v, {
        "Headers": "headers",
        "Result": "result",
      });
    }),
  );

export function patchSsfTransmitterTransmitterIdResponseFromJSON(
  jsonString: string,
): SafeParseResult<
  PatchSsfTransmitterTransmitterIdResponse,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      PatchSsfTransmitterTransmitterIdResponse$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'PatchSsfTransmitterTransmitterIdResponse' from JSON`,
  );
}
