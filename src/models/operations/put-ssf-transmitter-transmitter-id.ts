/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { ClosedEnum, OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

export type PutSsfTransmitterTransmitterIdAuthorizationSchemeRequest = {
  specUrn: string;
};

/**
 * OPTIONAL. A string indicating the default behavior of newly created streams. If present, the value MUST be either `"ALL"` or `"NONE"`. If not provided, the Transmitter behavior in this regard is unspecified.
 *
 * @remarks
 * - `"ALL"` indicates that any subjects that are appropriate for the
 *   stream are added to the stream by default. The Receiver MAY remove
 *   subjects from the stream via the `remove_subject_endpoint`, causing
 *   events for those subjects to _not_ be transmitted. The Receiver MAY
 *   re-add any subjects removed this way via the `add_subject_endpoint`.
 *
 * - `"NONE"` indicates that no subjects are added by default. The Receiver
 *   MAY add subjects to the stream via the `add_subject_endpoint`,
 *   causing only events for those subjects to be transmitted. The Receiver
 *   MAY remove subjects added this way via the `remove_subject_endpoint`.
 */
export const PutSsfTransmitterTransmitterIdDefaultSubjectsRequest = {
  All: "ALL",
  None: "NONE",
} as const;
/**
 * OPTIONAL. A string indicating the default behavior of newly created streams. If present, the value MUST be either `"ALL"` or `"NONE"`. If not provided, the Transmitter behavior in this regard is unspecified.
 *
 * @remarks
 * - `"ALL"` indicates that any subjects that are appropriate for the
 *   stream are added to the stream by default. The Receiver MAY remove
 *   subjects from the stream via the `remove_subject_endpoint`, causing
 *   events for those subjects to _not_ be transmitted. The Receiver MAY
 *   re-add any subjects removed this way via the `add_subject_endpoint`.
 *
 * - `"NONE"` indicates that no subjects are added by default. The Receiver
 *   MAY add subjects to the stream via the `add_subject_endpoint`,
 *   causing only events for those subjects to be transmitted. The Receiver
 *   MAY remove subjects added this way via the `remove_subject_endpoint`.
 */
export type PutSsfTransmitterTransmitterIdDefaultSubjectsRequest = ClosedEnum<
  typeof PutSsfTransmitterTransmitterIdDefaultSubjectsRequest
>;

/**
 * Transmitter Configuration Metadata defined in [OpenID Shared Signals Framework Specification 1.0](https://openid.net/specs/openid-sharedsignals-framework-1_0.html).
 *
 * @remarks
 */
export type PutSsfTransmitterTransmitterIdMetadataRequest = {
  /**
   * OPTIONAL. A version identifying the implementer's draft or final specification implemented by the Transmitter. This includes the numerical portion of the spec version as described in the document [[NAMINGCONVENTION](https://openid.net/wg/resources/naming-and-contents-of-specifications/)].
   *
   * @remarks
   */
  specVersion?: string | undefined;
  /**
   * REQUIRED. URL using the `https` scheme with no query or fragment component that the Transmitter asserts as its Issuer Identifier. This MUST be identical to the `iss` claim value in Security Event Tokens issued from this Transmitter.
   *
   * @remarks
   */
  issuer: string;
  /**
   * OPTIONAL. URL of the Transmitter's JSON Web Key Set [[RFC7517](https://www.rfc-editor.org/rfc/rfc7517.html)] document. This contains the signing key(s) the Receiver uses to validate signatures from the Transmitter. This value MUST be specified if the Transmitter intends to generate signed JWTs. If present, this URL MUST use HTTP over TLS [[RFC9110](https://www.rfc-editor.org/rfc/rfc9110.html)].
   *
   * @remarks
   */
  jwksUri?: string | undefined;
  /**
   * RECOMMENDED. List of supported delivery method URIs.
   *
   * @remarks
   */
  deliveryMethodsSupported?: Array<string> | undefined;
  /**
   * OPTIONAL. The URL of the Configuration Endpoint. If present, this URL MUST use HTTP over TLS [[RFC9110](https://www.rfc-editor.org/rfc/rfc9110.html)].
   *
   * @remarks
   */
  configurationEndpoint?: string | undefined;
  /**
   * OPTIONAL. The URL of the Status Endpoint. If present, this URL MUST use HTTP over TLS [[RFC9110](https://www.rfc-editor.org/rfc/rfc9110.html)].
   *
   * @remarks
   */
  statusEndpoint?: string | undefined;
  /**
   * OPTIONAL. The URL of the Add Subject Endpoint. If present, this URL MUST use HTTP over TLS [[RFC9110](https://www.rfc-editor.org/rfc/rfc9110.html)].
   *
   * @remarks
   */
  addSubjectEndpoint?: string | undefined;
  /**
   * OPTIONAL. The URL of the Remove Subject Endpoint. If present, this URL MUST use HTTP over TLS [[RFC9110](https://www.rfc-editor.org/rfc/rfc9110.html)].
   *
   * @remarks
   */
  removeSubjectEndpoint?: string | undefined;
  /**
   * OPTIONAL. The URL of the Verification Endpoint. If present, this URL MUST use HTTP over TLS [[RFC9110](https://www.rfc-editor.org/rfc/rfc9110.html)].
   *
   * @remarks
   */
  verificationEndpoint?: string | undefined;
  /**
   * OPTIONAL. An array of member names in a Complex Subject which, if present in a Subject Member in an event, MUST be interpreted by a Receiver.
   *
   * @remarks
   */
  criticalSubjectMembers?: Array<string> | undefined;
  /**
   * OPTIONAL. An array of JSON objects that specify the supported authorization scheme properties. To enable seamless discovery of configurations, the service provider SHOULD, with the appropriate security considerations, make the `authorization_schemes` attribute publicly accessible without prior authentication.
   *
   * @remarks
   */
  authorizationSchemes?:
    | Array<PutSsfTransmitterTransmitterIdAuthorizationSchemeRequest>
    | undefined;
  /**
   * OPTIONAL. A string indicating the default behavior of newly created streams. If present, the value MUST be either `"ALL"` or `"NONE"`. If not provided, the Transmitter behavior in this regard is unspecified.
   *
   * @remarks
   * - `"ALL"` indicates that any subjects that are appropriate for the
   *   stream are added to the stream by default. The Receiver MAY remove
   *   subjects from the stream via the `remove_subject_endpoint`, causing
   *   events for those subjects to _not_ be transmitted. The Receiver MAY
   *   re-add any subjects removed this way via the `add_subject_endpoint`.
   *
   * - `"NONE"` indicates that no subjects are added by default. The Receiver
   *   MAY add subjects to the stream via the `add_subject_endpoint`,
   *   causing only events for those subjects to be transmitted. The Receiver
   *   MAY remove subjects added this way via the `remove_subject_endpoint`.
   */
  defaultSubjects?:
    | PutSsfTransmitterTransmitterIdDefaultSubjectsRequest
    | undefined;
};

/**
 * The HTTP authentication scheme.
 */
export const PutSsfTransmitterTransmitterIdSchemeRequest = {
  Basic: "Basic",
  Bearer: "Bearer",
  DPoP: "DPoP",
} as const;
/**
 * The HTTP authentication scheme.
 */
export type PutSsfTransmitterTransmitterIdSchemeRequest = ClosedEnum<
  typeof PutSsfTransmitterTransmitterIdSchemeRequest
>;

/**
 * Configuration for access token introspection ([RFC 7662](https://www.rfc-editor.org/rfc/rfc7662.html)).
 *
 * @remarks
 */
export type PutSsfTransmitterTransmitterIdIntrospectionConfigurationRequest = {
  /**
   * The HTTP authentication scheme.
   */
  scheme?: PutSsfTransmitterTransmitterIdSchemeRequest | undefined;
  /**
   * The user ID, required for the Basic Authentication.
   */
  userId?: string | undefined;
  /**
   * The password, required for the Basic Authentication.
   */
  password?: string | undefined;
  /**
   * The access token, required for the Bearer or DPoP Authentication.
   */
  accessToken?: string | undefined;
  /**
   * The identifier of the authorization server.
   */
  issuer: string;
};

export type PutSsfTransmitterTransmitterIdSettingsRequest = {
  /**
   * The name of this Transmitter
   */
  name?: string | undefined;
  /**
   * The description about this Transmitter
   */
  description?: string | undefined;
  /**
   * This boolean flag indicates whether FAPI 2.0 compliance is required.
   *
   * @remarks
   *
   * If this flag is set to `true`, the requirements defined in the
   * [FAPI 2.0 Security Profile](https://openid.net/specs/fapi-security-profile-2_0-final.html)
   * are enforced. One notable requirement is that access tokens must
   * be sender-constrained using either MTLS
   * ([RFC 8705](https://www.rfc-editor.org/rfc/rfc8705.html)) or DPoP
   * ([RFC 9449](https://www.rfc-editor.org/rfc/rfc9449.html)).
   */
  fapi2Compliant?: boolean | undefined;
  /**
   * This boolean flag indicates whether access tokens must be
   *
   * @remarks
   * sender-constrained using the DPoP
   * ([RFC 9449](https://www.rfc-editor.org/rfc/rfc9449.html)) mechanism.
   */
  dpopRequired?: boolean | undefined;
  /**
   * This boolean flag indicates whether DPoP proof JWTs must include the
   *
   * @remarks
   * `nonce` claim.
   *
   * If this flag is set to `true`, the DPoP proof JWT presented with a
   * DPoP-key-bound access token must include the `nonce` claim. Otherwise,
   * the server will return a `401 Unauthorized` response with the error
   * code `use_dpop_nonce` and a `DPoP-Nonce` header. If the client receives
   * this error, it must regenerate a DPoP proof JWT including the `nonce`
   * claim, using the nonce value specified in the `DPop-Nonce` header, and
   * retry the request.
   *
   * Also note that the nonce value expires periodically. This means that
   * the client will periodically receive a `401 Unauthorized` response
   * with the error code `invalid_dpop_proof` and a `DPoP-Nonce` header.
   * If the client receives this error, it must regenerate a DPoP proof JWT
   * including the `nonce` claim with the updated nonce value specified in
   * the `DPoP-Nonce` header and retry the request.
   *
   * See [RFC 9449](https://www.rfc-editor.org/rfc/rfc9449.html) for details
   * about the DPoP nonce.
   */
  dpopNonceRequired?: boolean | undefined;
  /**
   * The maximum clock skew in seconds. This value is taken into account
   *
   * @remarks
   * when validating time-related properties, such as the expiration time
   * of an access token.
   *
   * Note that when FAPI 2.0 compliance is required (i.e., when the
   * `fapi2_compliant` flag is set to `true`), the maximum clock skew is
   * adjusted to ensure that it falls within the range of 10 to 60 seconds.
   */
  maxClockSkewSeconds?: number | undefined;
  /**
   * The list of authorization servers this Transmitter queries for access token information.
   *
   * @remarks
   */
  authorizationServers?: Array<string> | undefined;
  /**
   * The list of configurations for access token introspection.
   *
   * @remarks
   */
  introspectionConfigurations?:
    | Array<PutSsfTransmitterTransmitterIdIntrospectionConfigurationRequest>
    | undefined;
  /**
   * OPTIONAL. An array of URIs identifying the set of events supported by the Transmitter for a Receiver. If omitted, Event Transmitters SHOULD make this set available to the Event Receiver via some other means (e.g. publishing it in online documentation).
   *
   * @remarks
   */
  eventsSupported?: Array<string> | undefined;
  /**
   * The default value of `min_verification_interval` in a Stream configuration.
   *
   * @remarks
   */
  minVerificationIntervalDefault?: number | undefined;
  /**
   * The default value of `inactivity_timeout` in a Stream configuration.
   *
   * @remarks
   */
  inactivityTimeoutDefault?: number | undefined;
  /**
   * OPTIONAL. The URL of the Poll Endpoint.
   *
   * @remarks
   *
   * If the receiver specifies the delivery method as POLL (`urn:ietf:rfc:8936`)
   * in the stream creation request, the value of this property is used as the
   * `endpoint_url`. If the property value contains `STREAM_ID`, it will be
   * replaced with the newly issued stream ID.
   */
  pollEndpoint?: string | undefined;
};

export type PutSsfTransmitterTransmitterIdRequestBody = {
  /**
   * Transmitter Configuration Metadata defined in [OpenID Shared Signals Framework Specification 1.0](https://openid.net/specs/openid-sharedsignals-framework-1_0.html).
   *
   * @remarks
   */
  metadata: PutSsfTransmitterTransmitterIdMetadataRequest;
  settings?: PutSsfTransmitterTransmitterIdSettingsRequest | undefined;
};

export type PutSsfTransmitterTransmitterIdRequest = {
  /**
   * This header is defined in "[FAPI 1.0 Baseline](https://openid.net/specs/openid-financial-api-part-1-1_0.html)" and "FAPI 2.0 Implementation Advice". If the request includes this header, the response will contain a response header with the same name and the same value. If the request does not include this header, the value of the corresponding response header will be a randomly generated value. This mechanism is designed for tracking communication. When using this header, its value must be a UUID as defined in [RFC 4122](https://www.rfc-editor.org/rfc/rfc4122.html).
   *
   * @remarks
   */
  xFapiInteractionId?: string | undefined;
  /**
   * This query parameter enables pretty-printing when the response content type is JSON. If `pretty=true` (case-insensitive), the response is formatted for readability. If `pretty` is omitted or set to any other value, pretty-printing is disabled.
   *
   * @remarks
   */
  pretty?: boolean | undefined;
  /**
   * The transmitter ID.
   *
   * @remarks
   */
  transmitterId: string;
  body?: PutSsfTransmitterTransmitterIdRequestBody | undefined;
};

export const PutSsfTransmitterTransmitterIdNotFoundStatus = {
  Success: "success",
  Failure: "failure",
  BadRequest: "bad_request",
  Unauthorized: "unauthorized",
  Forbidden: "forbidden",
  NotFound: "not_found",
  Conflict: "conflict",
  ServerError: "server_error",
  ServiceUnavailable: "service_unavailable",
} as const;
export type PutSsfTransmitterTransmitterIdNotFoundStatus = OpenEnum<
  typeof PutSsfTransmitterTransmitterIdNotFoundStatus
>;

export type PutSsfTransmitterTransmitterIdNotFoundResult = {
  status?: PutSsfTransmitterTransmitterIdNotFoundStatus | undefined;
  code?: string | undefined;
  message?: string | undefined;
};

export const PutSsfTransmitterTransmitterIdStatus = {
  Success: "success",
  Failure: "failure",
  BadRequest: "bad_request",
  Unauthorized: "unauthorized",
  Forbidden: "forbidden",
  NotFound: "not_found",
  Conflict: "conflict",
  ServerError: "server_error",
  ServiceUnavailable: "service_unavailable",
} as const;
export type PutSsfTransmitterTransmitterIdStatus = OpenEnum<
  typeof PutSsfTransmitterTransmitterIdStatus
>;

export type PutSsfTransmitterTransmitterIdResult = {
  status?: PutSsfTransmitterTransmitterIdStatus | undefined;
  code?: string | undefined;
  message?: string | undefined;
};

/**
 * The creator of the transmitter.
 */
export type PutSsfTransmitterTransmitterIdCreator = {
  /**
   * The issuer of the access token.
   */
  iss?: string | undefined;
  /**
   * The subject of the access token.
   */
  sub?: string | undefined;
  /**
   * The client ID of the access token.
   */
  clientId?: string | undefined;
  /**
   * The identifier of the access token.
   */
  jti?: string | undefined;
  /**
   * The base64url-encoded SHA-256 hash of the access token.
   */
  hash?: string | undefined;
};

export type PutSsfTransmitterTransmitterIdAuthorizationSchemeResponse = {
  specUrn: string;
};

/**
 * OPTIONAL. A string indicating the default behavior of newly created streams. If present, the value MUST be either `"ALL"` or `"NONE"`. If not provided, the Transmitter behavior in this regard is unspecified.
 *
 * @remarks
 * - `"ALL"` indicates that any subjects that are appropriate for the
 *   stream are added to the stream by default. The Receiver MAY remove
 *   subjects from the stream via the `remove_subject_endpoint`, causing
 *   events for those subjects to _not_ be transmitted. The Receiver MAY
 *   re-add any subjects removed this way via the `add_subject_endpoint`.
 *
 * - `"NONE"` indicates that no subjects are added by default. The Receiver
 *   MAY add subjects to the stream via the `add_subject_endpoint`,
 *   causing only events for those subjects to be transmitted. The Receiver
 *   MAY remove subjects added this way via the `remove_subject_endpoint`.
 */
export const PutSsfTransmitterTransmitterIdTransmitterDefaultSubjects = {
  All: "ALL",
  None: "NONE",
} as const;
/**
 * OPTIONAL. A string indicating the default behavior of newly created streams. If present, the value MUST be either `"ALL"` or `"NONE"`. If not provided, the Transmitter behavior in this regard is unspecified.
 *
 * @remarks
 * - `"ALL"` indicates that any subjects that are appropriate for the
 *   stream are added to the stream by default. The Receiver MAY remove
 *   subjects from the stream via the `remove_subject_endpoint`, causing
 *   events for those subjects to _not_ be transmitted. The Receiver MAY
 *   re-add any subjects removed this way via the `add_subject_endpoint`.
 *
 * - `"NONE"` indicates that no subjects are added by default. The Receiver
 *   MAY add subjects to the stream via the `add_subject_endpoint`,
 *   causing only events for those subjects to be transmitted. The Receiver
 *   MAY remove subjects added this way via the `remove_subject_endpoint`.
 */
export type PutSsfTransmitterTransmitterIdTransmitterDefaultSubjects = OpenEnum<
  typeof PutSsfTransmitterTransmitterIdTransmitterDefaultSubjects
>;

/**
 * Transmitter Configuration Metadata defined in [OpenID Shared Signals Framework Specification 1.0](https://openid.net/specs/openid-sharedsignals-framework-1_0.html).
 *
 * @remarks
 */
export type PutSsfTransmitterTransmitterIdTransmitterMetadata = {
  /**
   * OPTIONAL. A version identifying the implementer's draft or final specification implemented by the Transmitter. This includes the numerical portion of the spec version as described in the document [[NAMINGCONVENTION](https://openid.net/wg/resources/naming-and-contents-of-specifications/)].
   *
   * @remarks
   */
  specVersion?: string | undefined;
  /**
   * REQUIRED. URL using the `https` scheme with no query or fragment component that the Transmitter asserts as its Issuer Identifier. This MUST be identical to the `iss` claim value in Security Event Tokens issued from this Transmitter.
   *
   * @remarks
   */
  issuer: string;
  /**
   * OPTIONAL. URL of the Transmitter's JSON Web Key Set [[RFC7517](https://www.rfc-editor.org/rfc/rfc7517.html)] document. This contains the signing key(s) the Receiver uses to validate signatures from the Transmitter. This value MUST be specified if the Transmitter intends to generate signed JWTs. If present, this URL MUST use HTTP over TLS [[RFC9110](https://www.rfc-editor.org/rfc/rfc9110.html)].
   *
   * @remarks
   */
  jwksUri?: string | undefined;
  /**
   * RECOMMENDED. List of supported delivery method URIs.
   *
   * @remarks
   */
  deliveryMethodsSupported?: Array<string> | undefined;
  /**
   * OPTIONAL. The URL of the Configuration Endpoint. If present, this URL MUST use HTTP over TLS [[RFC9110](https://www.rfc-editor.org/rfc/rfc9110.html)].
   *
   * @remarks
   */
  configurationEndpoint?: string | undefined;
  /**
   * OPTIONAL. The URL of the Status Endpoint. If present, this URL MUST use HTTP over TLS [[RFC9110](https://www.rfc-editor.org/rfc/rfc9110.html)].
   *
   * @remarks
   */
  statusEndpoint?: string | undefined;
  /**
   * OPTIONAL. The URL of the Add Subject Endpoint. If present, this URL MUST use HTTP over TLS [[RFC9110](https://www.rfc-editor.org/rfc/rfc9110.html)].
   *
   * @remarks
   */
  addSubjectEndpoint?: string | undefined;
  /**
   * OPTIONAL. The URL of the Remove Subject Endpoint. If present, this URL MUST use HTTP over TLS [[RFC9110](https://www.rfc-editor.org/rfc/rfc9110.html)].
   *
   * @remarks
   */
  removeSubjectEndpoint?: string | undefined;
  /**
   * OPTIONAL. The URL of the Verification Endpoint. If present, this URL MUST use HTTP over TLS [[RFC9110](https://www.rfc-editor.org/rfc/rfc9110.html)].
   *
   * @remarks
   */
  verificationEndpoint?: string | undefined;
  /**
   * OPTIONAL. An array of member names in a Complex Subject which, if present in a Subject Member in an event, MUST be interpreted by a Receiver.
   *
   * @remarks
   */
  criticalSubjectMembers?: Array<string> | undefined;
  /**
   * OPTIONAL. An array of JSON objects that specify the supported authorization scheme properties. To enable seamless discovery of configurations, the service provider SHOULD, with the appropriate security considerations, make the `authorization_schemes` attribute publicly accessible without prior authentication.
   *
   * @remarks
   */
  authorizationSchemes?:
    | Array<PutSsfTransmitterTransmitterIdAuthorizationSchemeResponse>
    | undefined;
  /**
   * OPTIONAL. A string indicating the default behavior of newly created streams. If present, the value MUST be either `"ALL"` or `"NONE"`. If not provided, the Transmitter behavior in this regard is unspecified.
   *
   * @remarks
   * - `"ALL"` indicates that any subjects that are appropriate for the
   *   stream are added to the stream by default. The Receiver MAY remove
   *   subjects from the stream via the `remove_subject_endpoint`, causing
   *   events for those subjects to _not_ be transmitted. The Receiver MAY
   *   re-add any subjects removed this way via the `add_subject_endpoint`.
   *
   * - `"NONE"` indicates that no subjects are added by default. The Receiver
   *   MAY add subjects to the stream via the `add_subject_endpoint`,
   *   causing only events for those subjects to be transmitted. The Receiver
   *   MAY remove subjects added this way via the `remove_subject_endpoint`.
   */
  defaultSubjects: PutSsfTransmitterTransmitterIdTransmitterDefaultSubjects;
};

/**
 * The HTTP authentication scheme.
 */
export const PutSsfTransmitterTransmitterIdSchemeResponse = {
  Basic: "Basic",
  Bearer: "Bearer",
  DPoP: "DPoP",
} as const;
/**
 * The HTTP authentication scheme.
 */
export type PutSsfTransmitterTransmitterIdSchemeResponse = OpenEnum<
  typeof PutSsfTransmitterTransmitterIdSchemeResponse
>;

/**
 * Configuration for access token introspection ([RFC 7662](https://www.rfc-editor.org/rfc/rfc7662.html)).
 *
 * @remarks
 */
export type PutSsfTransmitterTransmitterIdIntrospectionConfigurationResponse = {
  /**
   * The HTTP authentication scheme.
   */
  scheme?: PutSsfTransmitterTransmitterIdSchemeResponse | undefined;
  /**
   * The user ID, required for the Basic Authentication.
   */
  userId?: string | undefined;
  /**
   * The password, required for the Basic Authentication.
   */
  password?: string | undefined;
  /**
   * The access token, required for the Bearer or DPoP Authentication.
   */
  accessToken?: string | undefined;
  /**
   * The identifier of the authorization server.
   */
  issuer: string;
};

export type PutSsfTransmitterTransmitterIdSettingsResponse = {
  /**
   * The name of this Transmitter
   */
  name?: string | undefined;
  /**
   * The description about this Transmitter
   */
  description?: string | undefined;
  /**
   * This boolean flag indicates whether FAPI 2.0 compliance is required.
   *
   * @remarks
   *
   * If this flag is set to `true`, the requirements defined in the
   * [FAPI 2.0 Security Profile](https://openid.net/specs/fapi-security-profile-2_0-final.html)
   * are enforced. One notable requirement is that access tokens must
   * be sender-constrained using either MTLS
   * ([RFC 8705](https://www.rfc-editor.org/rfc/rfc8705.html)) or DPoP
   * ([RFC 9449](https://www.rfc-editor.org/rfc/rfc9449.html)).
   */
  fapi2Compliant: boolean;
  /**
   * This boolean flag indicates whether access tokens must be
   *
   * @remarks
   * sender-constrained using the DPoP
   * ([RFC 9449](https://www.rfc-editor.org/rfc/rfc9449.html)) mechanism.
   */
  dpopRequired: boolean;
  /**
   * This boolean flag indicates whether DPoP proof JWTs must include the
   *
   * @remarks
   * `nonce` claim.
   *
   * If this flag is set to `true`, the DPoP proof JWT presented with a
   * DPoP-key-bound access token must include the `nonce` claim. Otherwise,
   * the server will return a `401 Unauthorized` response with the error
   * code `use_dpop_nonce` and a `DPoP-Nonce` header. If the client receives
   * this error, it must regenerate a DPoP proof JWT including the `nonce`
   * claim, using the nonce value specified in the `DPop-Nonce` header, and
   * retry the request.
   *
   * Also note that the nonce value expires periodically. This means that
   * the client will periodically receive a `401 Unauthorized` response
   * with the error code `invalid_dpop_proof` and a `DPoP-Nonce` header.
   * If the client receives this error, it must regenerate a DPoP proof JWT
   * including the `nonce` claim with the updated nonce value specified in
   * the `DPoP-Nonce` header and retry the request.
   *
   * See [RFC 9449](https://www.rfc-editor.org/rfc/rfc9449.html) for details
   * about the DPoP nonce.
   */
  dpopNonceRequired: boolean;
  /**
   * The maximum clock skew in seconds. This value is taken into account
   *
   * @remarks
   * when validating time-related properties, such as the expiration time
   * of an access token.
   *
   * Note that when FAPI 2.0 compliance is required (i.e., when the
   * `fapi2_compliant` flag is set to `true`), the maximum clock skew is
   * adjusted to ensure that it falls within the range of 10 to 60 seconds.
   */
  maxClockSkewSeconds?: number | undefined;
  /**
   * The list of authorization servers this Transmitter queries for access token information.
   *
   * @remarks
   */
  authorizationServers?: Array<string> | undefined;
  /**
   * The list of configurations for access token introspection.
   *
   * @remarks
   */
  introspectionConfigurations?:
    | Array<PutSsfTransmitterTransmitterIdIntrospectionConfigurationResponse>
    | undefined;
  /**
   * OPTIONAL. An array of URIs identifying the set of events supported by the Transmitter for a Receiver. If omitted, Event Transmitters SHOULD make this set available to the Event Receiver via some other means (e.g. publishing it in online documentation).
   *
   * @remarks
   */
  eventsSupported?: Array<string> | undefined;
  /**
   * The default value of `min_verification_interval` in a Stream configuration.
   *
   * @remarks
   */
  minVerificationIntervalDefault?: number | undefined;
  /**
   * The default value of `inactivity_timeout` in a Stream configuration.
   *
   * @remarks
   */
  inactivityTimeoutDefault?: number | undefined;
  /**
   * OPTIONAL. The URL of the Poll Endpoint.
   *
   * @remarks
   *
   * If the receiver specifies the delivery method as POLL (`urn:ietf:rfc:8936`)
   * in the stream creation request, the value of this property is used as the
   * `endpoint_url`. If the property value contains `STREAM_ID`, it will be
   * replaced with the newly issued stream ID.
   */
  pollEndpoint?: string | undefined;
};

export type PutSsfTransmitterTransmitterIdTransmitter = {
  /**
   * The time at which the transmitter was created, in the format of `YYYY-MM-DDThh:mm:ss`.
   *
   * @remarks
   */
  createdAt?: Date | undefined;
  /**
   * The time at which the transmitter was updated, in the format of `YYYY-MM-DDThh:mm:ss`.
   *
   * @remarks
   */
  updatedAt?: Date | undefined;
  /**
   * The creator of the transmitter.
   */
  creator?: PutSsfTransmitterTransmitterIdCreator | undefined;
  /**
   * The Transmitter ID assigned by the Arena Core Server. The value has 240-bit entropy and is encoded in the base32hex format, resulting in a 48-character lowercase string.
   *
   * @remarks
   * Strictly speaking, since the first 10 bits of the data are overwritten to ensure that the resulting string always starts with `tr`, the actual entropy is 230 bits. The `tr` here denotes Transmitter.
   * The purpose of this process is to enable someone encountering a 48-character base32hex-encoded string beginning with `tr` to infer that it might be a Transmitter ID. Conversely, a random string not starting with `tr` can be identified as not being a Transmitter ID.
   */
  transmitterId?: string | undefined;
  /**
   * Transmitter Configuration Metadata defined in [OpenID Shared Signals Framework Specification 1.0](https://openid.net/specs/openid-sharedsignals-framework-1_0.html).
   *
   * @remarks
   */
  metadata: PutSsfTransmitterTransmitterIdTransmitterMetadata;
  settings?: PutSsfTransmitterTransmitterIdSettingsResponse | undefined;
};

/**
 * Successfully updated the transmitter.
 */
export type PutSsfTransmitterTransmitterIdResponseBody = {
  result?: PutSsfTransmitterTransmitterIdResult | undefined;
  transmitter?: PutSsfTransmitterTransmitterIdTransmitter | undefined;
};

export type PutSsfTransmitterTransmitterIdResponse = {
  headers: { [k: string]: Array<string> };
  result: PutSsfTransmitterTransmitterIdResponseBody;
};

/** @internal */
export type PutSsfTransmitterTransmitterIdAuthorizationSchemeRequest$Outbound =
  {
    spec_urn: string;
  };

/** @internal */
export const PutSsfTransmitterTransmitterIdAuthorizationSchemeRequest$outboundSchema:
  z.ZodMiniType<
    PutSsfTransmitterTransmitterIdAuthorizationSchemeRequest$Outbound,
    PutSsfTransmitterTransmitterIdAuthorizationSchemeRequest
  > = z.pipe(
    z.object({
      specUrn: z.string(),
    }),
    z.transform((v) => {
      return remap$(v, {
        specUrn: "spec_urn",
      });
    }),
  );

export function putSsfTransmitterTransmitterIdAuthorizationSchemeRequestToJSON(
  putSsfTransmitterTransmitterIdAuthorizationSchemeRequest:
    PutSsfTransmitterTransmitterIdAuthorizationSchemeRequest,
): string {
  return JSON.stringify(
    PutSsfTransmitterTransmitterIdAuthorizationSchemeRequest$outboundSchema
      .parse(putSsfTransmitterTransmitterIdAuthorizationSchemeRequest),
  );
}

/** @internal */
export const PutSsfTransmitterTransmitterIdDefaultSubjectsRequest$outboundSchema:
  z.ZodMiniEnum<typeof PutSsfTransmitterTransmitterIdDefaultSubjectsRequest> = z
    .enum(PutSsfTransmitterTransmitterIdDefaultSubjectsRequest);

/** @internal */
export type PutSsfTransmitterTransmitterIdMetadataRequest$Outbound = {
  spec_version?: string | undefined;
  issuer: string;
  jwks_uri?: string | undefined;
  delivery_methods_supported?: Array<string> | undefined;
  configuration_endpoint?: string | undefined;
  status_endpoint?: string | undefined;
  add_subject_endpoint?: string | undefined;
  remove_subject_endpoint?: string | undefined;
  verification_endpoint?: string | undefined;
  critical_subject_members?: Array<string> | undefined;
  authorization_schemes?:
    | Array<PutSsfTransmitterTransmitterIdAuthorizationSchemeRequest$Outbound>
    | undefined;
  default_subjects: string;
};

/** @internal */
export const PutSsfTransmitterTransmitterIdMetadataRequest$outboundSchema:
  z.ZodMiniType<
    PutSsfTransmitterTransmitterIdMetadataRequest$Outbound,
    PutSsfTransmitterTransmitterIdMetadataRequest
  > = z.pipe(
    z.object({
      specVersion: z.optional(z.string()),
      issuer: z.string(),
      jwksUri: z.optional(z.string()),
      deliveryMethodsSupported: z.optional(z.array(z.string())),
      configurationEndpoint: z.optional(z.string()),
      statusEndpoint: z.optional(z.string()),
      addSubjectEndpoint: z.optional(z.string()),
      removeSubjectEndpoint: z.optional(z.string()),
      verificationEndpoint: z.optional(z.string()),
      criticalSubjectMembers: z.optional(z.array(z.string())),
      authorizationSchemes: z.optional(z.array(z.lazy(() =>
        PutSsfTransmitterTransmitterIdAuthorizationSchemeRequest$outboundSchema
      ))),
      defaultSubjects: z._default(
        PutSsfTransmitterTransmitterIdDefaultSubjectsRequest$outboundSchema,
        "NONE",
      ),
    }),
    z.transform((v) => {
      return remap$(v, {
        specVersion: "spec_version",
        jwksUri: "jwks_uri",
        deliveryMethodsSupported: "delivery_methods_supported",
        configurationEndpoint: "configuration_endpoint",
        statusEndpoint: "status_endpoint",
        addSubjectEndpoint: "add_subject_endpoint",
        removeSubjectEndpoint: "remove_subject_endpoint",
        verificationEndpoint: "verification_endpoint",
        criticalSubjectMembers: "critical_subject_members",
        authorizationSchemes: "authorization_schemes",
        defaultSubjects: "default_subjects",
      });
    }),
  );

export function putSsfTransmitterTransmitterIdMetadataRequestToJSON(
  putSsfTransmitterTransmitterIdMetadataRequest:
    PutSsfTransmitterTransmitterIdMetadataRequest,
): string {
  return JSON.stringify(
    PutSsfTransmitterTransmitterIdMetadataRequest$outboundSchema.parse(
      putSsfTransmitterTransmitterIdMetadataRequest,
    ),
  );
}

/** @internal */
export const PutSsfTransmitterTransmitterIdSchemeRequest$outboundSchema:
  z.ZodMiniEnum<typeof PutSsfTransmitterTransmitterIdSchemeRequest> = z.enum(
    PutSsfTransmitterTransmitterIdSchemeRequest,
  );

/** @internal */
export type PutSsfTransmitterTransmitterIdIntrospectionConfigurationRequest$Outbound =
  {
    scheme?: string | undefined;
    user_id?: string | undefined;
    password?: string | undefined;
    access_token?: string | undefined;
    issuer: string;
  };

/** @internal */
export const PutSsfTransmitterTransmitterIdIntrospectionConfigurationRequest$outboundSchema:
  z.ZodMiniType<
    PutSsfTransmitterTransmitterIdIntrospectionConfigurationRequest$Outbound,
    PutSsfTransmitterTransmitterIdIntrospectionConfigurationRequest
  > = z.pipe(
    z.object({
      scheme: z.optional(
        PutSsfTransmitterTransmitterIdSchemeRequest$outboundSchema,
      ),
      userId: z.optional(z.string()),
      password: z.optional(z.string()),
      accessToken: z.optional(z.string()),
      issuer: z.string(),
    }),
    z.transform((v) => {
      return remap$(v, {
        userId: "user_id",
        accessToken: "access_token",
      });
    }),
  );

export function putSsfTransmitterTransmitterIdIntrospectionConfigurationRequestToJSON(
  putSsfTransmitterTransmitterIdIntrospectionConfigurationRequest:
    PutSsfTransmitterTransmitterIdIntrospectionConfigurationRequest,
): string {
  return JSON.stringify(
    PutSsfTransmitterTransmitterIdIntrospectionConfigurationRequest$outboundSchema
      .parse(putSsfTransmitterTransmitterIdIntrospectionConfigurationRequest),
  );
}

/** @internal */
export type PutSsfTransmitterTransmitterIdSettingsRequest$Outbound = {
  name?: string | undefined;
  description?: string | undefined;
  fapi2_compliant: boolean;
  dpop_required: boolean;
  dpop_nonce_required: boolean;
  max_clock_skew_seconds?: number | undefined;
  authorization_servers?: Array<string> | undefined;
  introspection_configurations?:
    | Array<
      PutSsfTransmitterTransmitterIdIntrospectionConfigurationRequest$Outbound
    >
    | undefined;
  events_supported?: Array<string> | undefined;
  min_verification_interval_default?: number | undefined;
  inactivity_timeout_default?: number | undefined;
  poll_endpoint?: string | undefined;
};

/** @internal */
export const PutSsfTransmitterTransmitterIdSettingsRequest$outboundSchema:
  z.ZodMiniType<
    PutSsfTransmitterTransmitterIdSettingsRequest$Outbound,
    PutSsfTransmitterTransmitterIdSettingsRequest
  > = z.pipe(
    z.object({
      name: z.optional(z.string()),
      description: z.optional(z.string()),
      fapi2Compliant: z._default(z.boolean(), false),
      dpopRequired: z._default(z.boolean(), false),
      dpopNonceRequired: z._default(z.boolean(), false),
      maxClockSkewSeconds: z.optional(z.int()),
      authorizationServers: z.optional(z.array(z.string())),
      introspectionConfigurations: z.optional(z.array(z.lazy(() =>
        PutSsfTransmitterTransmitterIdIntrospectionConfigurationRequest$outboundSchema
      ))),
      eventsSupported: z.optional(z.array(z.string())),
      minVerificationIntervalDefault: z.optional(z.int()),
      inactivityTimeoutDefault: z.optional(z.int()),
      pollEndpoint: z.optional(z.string()),
    }),
    z.transform((v) => {
      return remap$(v, {
        fapi2Compliant: "fapi2_compliant",
        dpopRequired: "dpop_required",
        dpopNonceRequired: "dpop_nonce_required",
        maxClockSkewSeconds: "max_clock_skew_seconds",
        authorizationServers: "authorization_servers",
        introspectionConfigurations: "introspection_configurations",
        eventsSupported: "events_supported",
        minVerificationIntervalDefault: "min_verification_interval_default",
        inactivityTimeoutDefault: "inactivity_timeout_default",
        pollEndpoint: "poll_endpoint",
      });
    }),
  );

export function putSsfTransmitterTransmitterIdSettingsRequestToJSON(
  putSsfTransmitterTransmitterIdSettingsRequest:
    PutSsfTransmitterTransmitterIdSettingsRequest,
): string {
  return JSON.stringify(
    PutSsfTransmitterTransmitterIdSettingsRequest$outboundSchema.parse(
      putSsfTransmitterTransmitterIdSettingsRequest,
    ),
  );
}

/** @internal */
export type PutSsfTransmitterTransmitterIdRequestBody$Outbound = {
  metadata: PutSsfTransmitterTransmitterIdMetadataRequest$Outbound;
  settings?: PutSsfTransmitterTransmitterIdSettingsRequest$Outbound | undefined;
};

/** @internal */
export const PutSsfTransmitterTransmitterIdRequestBody$outboundSchema:
  z.ZodMiniType<
    PutSsfTransmitterTransmitterIdRequestBody$Outbound,
    PutSsfTransmitterTransmitterIdRequestBody
  > = z.object({
    metadata: z.lazy(() =>
      PutSsfTransmitterTransmitterIdMetadataRequest$outboundSchema
    ),
    settings: z.optional(
      z.lazy(() =>
        PutSsfTransmitterTransmitterIdSettingsRequest$outboundSchema
      ),
    ),
  });

export function putSsfTransmitterTransmitterIdRequestBodyToJSON(
  putSsfTransmitterTransmitterIdRequestBody:
    PutSsfTransmitterTransmitterIdRequestBody,
): string {
  return JSON.stringify(
    PutSsfTransmitterTransmitterIdRequestBody$outboundSchema.parse(
      putSsfTransmitterTransmitterIdRequestBody,
    ),
  );
}

/** @internal */
export type PutSsfTransmitterTransmitterIdRequest$Outbound = {
  "x-fapi-interaction-id"?: string | undefined;
  pretty: boolean;
  transmitter_id: string;
  body?: PutSsfTransmitterTransmitterIdRequestBody$Outbound | undefined;
};

/** @internal */
export const PutSsfTransmitterTransmitterIdRequest$outboundSchema:
  z.ZodMiniType<
    PutSsfTransmitterTransmitterIdRequest$Outbound,
    PutSsfTransmitterTransmitterIdRequest
  > = z.pipe(
    z.object({
      xFapiInteractionId: z.optional(z.string()),
      pretty: z._default(z.boolean(), false),
      transmitterId: z.string(),
      body: z.optional(
        z.lazy(() => PutSsfTransmitterTransmitterIdRequestBody$outboundSchema),
      ),
    }),
    z.transform((v) => {
      return remap$(v, {
        xFapiInteractionId: "x-fapi-interaction-id",
        transmitterId: "transmitter_id",
      });
    }),
  );

export function putSsfTransmitterTransmitterIdRequestToJSON(
  putSsfTransmitterTransmitterIdRequest: PutSsfTransmitterTransmitterIdRequest,
): string {
  return JSON.stringify(
    PutSsfTransmitterTransmitterIdRequest$outboundSchema.parse(
      putSsfTransmitterTransmitterIdRequest,
    ),
  );
}

/** @internal */
export const PutSsfTransmitterTransmitterIdNotFoundStatus$inboundSchema:
  z.ZodMiniType<PutSsfTransmitterTransmitterIdNotFoundStatus, unknown> =
    openEnums.inboundSchema(PutSsfTransmitterTransmitterIdNotFoundStatus);

/** @internal */
export const PutSsfTransmitterTransmitterIdNotFoundResult$inboundSchema:
  z.ZodMiniType<PutSsfTransmitterTransmitterIdNotFoundResult, unknown> = z
    .object({
      status: types.optional(
        PutSsfTransmitterTransmitterIdNotFoundStatus$inboundSchema,
      ),
      code: types.optional(types.string()),
      message: types.optional(types.string()),
    });

export function putSsfTransmitterTransmitterIdNotFoundResultFromJSON(
  jsonString: string,
): SafeParseResult<
  PutSsfTransmitterTransmitterIdNotFoundResult,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      PutSsfTransmitterTransmitterIdNotFoundResult$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'PutSsfTransmitterTransmitterIdNotFoundResult' from JSON`,
  );
}

/** @internal */
export const PutSsfTransmitterTransmitterIdStatus$inboundSchema: z.ZodMiniType<
  PutSsfTransmitterTransmitterIdStatus,
  unknown
> = openEnums.inboundSchema(PutSsfTransmitterTransmitterIdStatus);

/** @internal */
export const PutSsfTransmitterTransmitterIdResult$inboundSchema: z.ZodMiniType<
  PutSsfTransmitterTransmitterIdResult,
  unknown
> = z.object({
  status: types.optional(PutSsfTransmitterTransmitterIdStatus$inboundSchema),
  code: types.optional(types.string()),
  message: types.optional(types.string()),
});

export function putSsfTransmitterTransmitterIdResultFromJSON(
  jsonString: string,
): SafeParseResult<PutSsfTransmitterTransmitterIdResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      PutSsfTransmitterTransmitterIdResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PutSsfTransmitterTransmitterIdResult' from JSON`,
  );
}

/** @internal */
export const PutSsfTransmitterTransmitterIdCreator$inboundSchema: z.ZodMiniType<
  PutSsfTransmitterTransmitterIdCreator,
  unknown
> = z.pipe(
  z.object({
    iss: types.optional(types.string()),
    sub: types.optional(types.string()),
    client_id: types.optional(types.string()),
    jti: types.optional(types.string()),
    hash: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "client_id": "clientId",
    });
  }),
);

export function putSsfTransmitterTransmitterIdCreatorFromJSON(
  jsonString: string,
): SafeParseResult<PutSsfTransmitterTransmitterIdCreator, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      PutSsfTransmitterTransmitterIdCreator$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PutSsfTransmitterTransmitterIdCreator' from JSON`,
  );
}

/** @internal */
export const PutSsfTransmitterTransmitterIdAuthorizationSchemeResponse$inboundSchema:
  z.ZodMiniType<
    PutSsfTransmitterTransmitterIdAuthorizationSchemeResponse,
    unknown
  > = z.pipe(
    z.object({
      spec_urn: types.string(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "spec_urn": "specUrn",
      });
    }),
  );

export function putSsfTransmitterTransmitterIdAuthorizationSchemeResponseFromJSON(
  jsonString: string,
): SafeParseResult<
  PutSsfTransmitterTransmitterIdAuthorizationSchemeResponse,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      PutSsfTransmitterTransmitterIdAuthorizationSchemeResponse$inboundSchema
        .parse(JSON.parse(x)),
    `Failed to parse 'PutSsfTransmitterTransmitterIdAuthorizationSchemeResponse' from JSON`,
  );
}

/** @internal */
export const PutSsfTransmitterTransmitterIdTransmitterDefaultSubjects$inboundSchema:
  z.ZodMiniType<
    PutSsfTransmitterTransmitterIdTransmitterDefaultSubjects,
    unknown
  > = openEnums.inboundSchema(
    PutSsfTransmitterTransmitterIdTransmitterDefaultSubjects,
  );

/** @internal */
export const PutSsfTransmitterTransmitterIdTransmitterMetadata$inboundSchema:
  z.ZodMiniType<PutSsfTransmitterTransmitterIdTransmitterMetadata, unknown> = z
    .pipe(
      z.object({
        spec_version: types.optional(types.string()),
        issuer: types.string(),
        jwks_uri: types.optional(types.string()),
        delivery_methods_supported: types.optional(z.array(types.string())),
        configuration_endpoint: types.optional(types.string()),
        status_endpoint: types.optional(types.string()),
        add_subject_endpoint: types.optional(types.string()),
        remove_subject_endpoint: types.optional(types.string()),
        verification_endpoint: types.optional(types.string()),
        critical_subject_members: types.optional(z.array(types.string())),
        authorization_schemes: types.optional(z.array(z.lazy(() =>
          PutSsfTransmitterTransmitterIdAuthorizationSchemeResponse$inboundSchema
        ))),
        default_subjects: z._default(
          PutSsfTransmitterTransmitterIdTransmitterDefaultSubjects$inboundSchema,
          "NONE",
        ),
      }),
      z.transform((v) => {
        return remap$(v, {
          "spec_version": "specVersion",
          "jwks_uri": "jwksUri",
          "delivery_methods_supported": "deliveryMethodsSupported",
          "configuration_endpoint": "configurationEndpoint",
          "status_endpoint": "statusEndpoint",
          "add_subject_endpoint": "addSubjectEndpoint",
          "remove_subject_endpoint": "removeSubjectEndpoint",
          "verification_endpoint": "verificationEndpoint",
          "critical_subject_members": "criticalSubjectMembers",
          "authorization_schemes": "authorizationSchemes",
          "default_subjects": "defaultSubjects",
        });
      }),
    );

export function putSsfTransmitterTransmitterIdTransmitterMetadataFromJSON(
  jsonString: string,
): SafeParseResult<
  PutSsfTransmitterTransmitterIdTransmitterMetadata,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      PutSsfTransmitterTransmitterIdTransmitterMetadata$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'PutSsfTransmitterTransmitterIdTransmitterMetadata' from JSON`,
  );
}

/** @internal */
export const PutSsfTransmitterTransmitterIdSchemeResponse$inboundSchema:
  z.ZodMiniType<PutSsfTransmitterTransmitterIdSchemeResponse, unknown> =
    openEnums.inboundSchema(PutSsfTransmitterTransmitterIdSchemeResponse);

/** @internal */
export const PutSsfTransmitterTransmitterIdIntrospectionConfigurationResponse$inboundSchema:
  z.ZodMiniType<
    PutSsfTransmitterTransmitterIdIntrospectionConfigurationResponse,
    unknown
  > = z.pipe(
    z.object({
      scheme: types.optional(
        PutSsfTransmitterTransmitterIdSchemeResponse$inboundSchema,
      ),
      user_id: types.optional(types.string()),
      password: types.optional(types.string()),
      access_token: types.optional(types.string()),
      issuer: types.string(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "user_id": "userId",
        "access_token": "accessToken",
      });
    }),
  );

export function putSsfTransmitterTransmitterIdIntrospectionConfigurationResponseFromJSON(
  jsonString: string,
): SafeParseResult<
  PutSsfTransmitterTransmitterIdIntrospectionConfigurationResponse,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      PutSsfTransmitterTransmitterIdIntrospectionConfigurationResponse$inboundSchema
        .parse(JSON.parse(x)),
    `Failed to parse 'PutSsfTransmitterTransmitterIdIntrospectionConfigurationResponse' from JSON`,
  );
}

/** @internal */
export const PutSsfTransmitterTransmitterIdSettingsResponse$inboundSchema:
  z.ZodMiniType<PutSsfTransmitterTransmitterIdSettingsResponse, unknown> = z
    .pipe(
      z.object({
        name: types.optional(types.string()),
        description: types.optional(types.string()),
        fapi2_compliant: z._default(types.boolean(), false),
        dpop_required: z._default(types.boolean(), false),
        dpop_nonce_required: z._default(types.boolean(), false),
        max_clock_skew_seconds: types.optional(types.number()),
        authorization_servers: types.optional(z.array(types.string())),
        introspection_configurations: types.optional(z.array(z.lazy(() =>
          PutSsfTransmitterTransmitterIdIntrospectionConfigurationResponse$inboundSchema
        ))),
        events_supported: types.optional(z.array(types.string())),
        min_verification_interval_default: types.optional(types.number()),
        inactivity_timeout_default: types.optional(types.number()),
        poll_endpoint: types.optional(types.string()),
      }),
      z.transform((v) => {
        return remap$(v, {
          "fapi2_compliant": "fapi2Compliant",
          "dpop_required": "dpopRequired",
          "dpop_nonce_required": "dpopNonceRequired",
          "max_clock_skew_seconds": "maxClockSkewSeconds",
          "authorization_servers": "authorizationServers",
          "introspection_configurations": "introspectionConfigurations",
          "events_supported": "eventsSupported",
          "min_verification_interval_default": "minVerificationIntervalDefault",
          "inactivity_timeout_default": "inactivityTimeoutDefault",
          "poll_endpoint": "pollEndpoint",
        });
      }),
    );

export function putSsfTransmitterTransmitterIdSettingsResponseFromJSON(
  jsonString: string,
): SafeParseResult<
  PutSsfTransmitterTransmitterIdSettingsResponse,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      PutSsfTransmitterTransmitterIdSettingsResponse$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'PutSsfTransmitterTransmitterIdSettingsResponse' from JSON`,
  );
}

/** @internal */
export const PutSsfTransmitterTransmitterIdTransmitter$inboundSchema:
  z.ZodMiniType<PutSsfTransmitterTransmitterIdTransmitter, unknown> = z.pipe(
    z.object({
      created_at: types.optional(types.date()),
      updated_at: types.optional(types.date()),
      creator: types.optional(z.lazy(() =>
        PutSsfTransmitterTransmitterIdCreator$inboundSchema
      )),
      transmitter_id: types.optional(types.string()),
      metadata: z.lazy(() =>
        PutSsfTransmitterTransmitterIdTransmitterMetadata$inboundSchema
      ),
      settings: types.optional(z.lazy(() =>
        PutSsfTransmitterTransmitterIdSettingsResponse$inboundSchema
      )),
    }),
    z.transform((v) => {
      return remap$(v, {
        "created_at": "createdAt",
        "updated_at": "updatedAt",
        "transmitter_id": "transmitterId",
      });
    }),
  );

export function putSsfTransmitterTransmitterIdTransmitterFromJSON(
  jsonString: string,
): SafeParseResult<
  PutSsfTransmitterTransmitterIdTransmitter,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      PutSsfTransmitterTransmitterIdTransmitter$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'PutSsfTransmitterTransmitterIdTransmitter' from JSON`,
  );
}

/** @internal */
export const PutSsfTransmitterTransmitterIdResponseBody$inboundSchema:
  z.ZodMiniType<PutSsfTransmitterTransmitterIdResponseBody, unknown> = z.object(
    {
      result: types.optional(z.lazy(() =>
        PutSsfTransmitterTransmitterIdResult$inboundSchema
      )),
      transmitter: types.optional(z.lazy(() =>
        PutSsfTransmitterTransmitterIdTransmitter$inboundSchema
      )),
    },
  );

export function putSsfTransmitterTransmitterIdResponseBodyFromJSON(
  jsonString: string,
): SafeParseResult<
  PutSsfTransmitterTransmitterIdResponseBody,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      PutSsfTransmitterTransmitterIdResponseBody$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'PutSsfTransmitterTransmitterIdResponseBody' from JSON`,
  );
}

/** @internal */
export const PutSsfTransmitterTransmitterIdResponse$inboundSchema:
  z.ZodMiniType<PutSsfTransmitterTransmitterIdResponse, unknown> = z.pipe(
    z.object({
      Headers: z._default(z.record(z.string(), z.array(z.string())), {}),
      Result: z.lazy(() =>
        PutSsfTransmitterTransmitterIdResponseBody$inboundSchema
      ),
    }),
    z.transform((v) => {
      return remap$(v, {
        "Headers": "headers",
        "Result": "result",
      });
    }),
  );

export function putSsfTransmitterTransmitterIdResponseFromJSON(
  jsonString: string,
): SafeParseResult<PutSsfTransmitterTransmitterIdResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      PutSsfTransmitterTransmitterIdResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PutSsfTransmitterTransmitterIdResponse' from JSON`,
  );
}
